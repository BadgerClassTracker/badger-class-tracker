# Reduce npm warnings
fund=false
audit=false

# Use legacy peer deps to reduce conflicts with Amplify
legacy-peer-deps=true

# Improve performance
prefer-offline=true
progress=false

# Reduce resolver warnings
strict-peer-deps=false